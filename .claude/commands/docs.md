Review and update documentation in `docs/` to reflect the current state of the codebase. Check that:

1. `CLAUDE.md` is accurate (commands, structure, conventions)
2. `docs/PROJECT_LAYOUT.md` matches the actual directory structure
3. `docs/API.md` matches the implemented endpoints
4. `docs/PATTERNS.md` reflects actual coding patterns used
5. `README.md` quickstart instructions work
