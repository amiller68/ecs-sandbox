{
  "name": "@ecs-sandbox/ecs-sandbox-agent",
  "private": true,
  "scripts": {
    "build": "make build",
    "docker-build": "make docker-build",
    "test": "make test",
    "check": "make check",
    "clean": "make clean"
  }
}
