# Worktree configuration
[worktree]
base = "origin/main"
on_create = "make install"
copy = [".env"]

# Spawn configuration
[spawn]
auto = true
auto_spawn = true

# Issues configuration
[issues]
spawn_labels = ["auto"]

# Agent configuration
[agent]
type = "claude"
