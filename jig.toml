# Worktree configuration
[worktree]
base = "origin/main"
on_create = "make install"
copy = [".env"]

# Spawn configuration
[spawn]
auto = true

# Issues configuration
[issues]
auto_spawn = true
spawn_labels = ["auto"]

# Agent configuration
[agent]
type = "claude"
